import React from "react";
// import PropTypes from "prop-types";
import { BrowserRouter as Router } from "react-router-dom";

const Press = () => {
  return (
    // <Router basename="/ppress">
    <h2>Press inside /press</h2>
    // </Router>
  );
};

export default Press;
